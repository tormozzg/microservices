eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka/
spring:
  application:
    name: encoding
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      fail-fast: true